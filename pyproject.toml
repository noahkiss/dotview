[project]
name = "dotview"
version = "0.1.0"
description = "Lightweight read-only file browser for git repos with syntax highlighting"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
]
